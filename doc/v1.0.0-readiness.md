# v1.0.0 Readiness Checklist

<!-- markdownlint-disable MD013 -->

Overall status: PASS (RC-ready) - v1.0.0-rc.1 soak period started 2026-06-27

This document defines the explicit criteria that must all be satisfied before
the `v1.0.0` tag is created. Each item maps to a Change-Freeze (CF) or
Refactoring (RF) item from the OraDBA roadmap.

**Owner:** Stefan Oehrli
**Target:** v1.0.0 release (after 30-day soak from v1.0.0-rc.1)

---

## Blocking Change-Freeze Items

All of the following CFs must be closed (resolved and merged to `main`):

- [x] CF-001 - No standalone `(( var++ ))` under `set -e`; zero matches in src/
- [x] CF-002 - `mktemp` + `EXIT` trap in `oradba_dbca.sh` (secure temp dir)
- [x] CF-003 - `plugin_check_listener_status()` defined in `plugin_interface.sh`
- [x] CF-004 - No direct plugin calls outside `execute_plugin_function_v2` / `plugin_interface`
- [x] CF-005 - `set -euo pipefail` in `oradba_homes.sh` and `oradba_extension.sh`
- [x] CF-006 - `sha256sum`/`shasum` checksum verification in `oradba_install.sh`
- [x] CF-007 - `ORADBA_BASE` canonical; `ORADBA_PREFIX` deprecation warning in bootstrap + common
- [x] CF-008 - `tests/test_oradba_env_validator.bats` exists and covers validation
- [x] CF-009 - Regression markers (`regression`/`CF-001`/`CF-002`/`CF-003`) >= 12 in tests/
- [x] CF-010 - `FILE_VERSION`/`TAG_VERSION` mismatch check in `.github/workflows/release.yml`
- [x] CF-015 - `ORADBA_LOAD_PDB_ALIASES` gates PDB alias generation
- [x] CF-017 - `oraenv.sh` delegates to `oradba_build_environment`
- [x] CF-018 - `OPEN|MOUNTED|NOMOUNT|STARTED|SHUTDOWN` constants in `oradba_env_status.sh`
- [x] CF-034 - `doc/v1.0.0-readiness.md` exists (this file)

---

## Quality Gate Checks

All of these must pass on the final commit before tagging:

- [x] `make lint` exits 0 (no shellcheck warnings, no markdownlint errors, shfmt clean)
- [ ] `make test-full` passes with 0 failures (skips are acceptable)
- [ ] `make validate-docs-counts` exits 0 (`.testmap.yml` count matches actual)
- [x] `awk '{if(length>120) print NR": "length" chars"}' CHANGELOG.md` - empty output for RC section

---

## Version and Release Artifacts

- [ ] `VERSION` file contains exactly `1.0.0` (currently `1.0.0-rc.1` - post-soak)
- [x] `CHANGELOG.md` has a `## [1.0.0-rc.1] - 2026-06-27` section
- [x] `doc/releases/v1.0.0-rc.1.md` exists and documents the RC
- [ ] `doc/releases/v1.0.0.md` exists (to be created after soak completes)

---

## Integration and Validation

- [ ] Docker integration run executed against Oracle AI Database 26ai
- [ ] Results recorded (pass/fail counts, environment, date)
- [ ] No Critical or High regressions found during the 30-day RC soak period
- [ ] RC soak start date recorded and 30 days elapsed since then

**RC soak start:** 2026-06-27
**RC soak end (earliest):** 2026-07-27

---

## API and Compatibility

- [x] Deprecation warnings present and tested for `ORADBA_PREFIX`
- [ ] Public API surface declared frozen in `doc/api.md` (marked as stable)
- [x] All public functions have `oradba_` prefix (old names kept as deprecation aliases)
- [x] No undocumented breaking changes since the last release

---

## How to Use This Document

1. Work through all items top-to-bottom; mark each `[x]` when resolved
2. For blocking CFs: verify the fix is merged to `main` and CI is green
3. For quality gates: run the commands and paste output into a comment
4. Only create the `v1.0.0` tag after ALL items are checked and 30-day soak complete

---

Document version: 1.1 - Updated for v1.0.0-rc.1 (2026-06-27)
