# Release Notes v0.20.2

**Release Date:** 2026-02-09  
**Type:** Patch Release (Data Safe Connector Fixes)

## Overview

Version 0.20.2 improves Data Safe connector isolation by enforcing
connector-specific `TNS_ADMIN` across environment setup and connector control
operations. It also adds tests to validate the new behavior.

## ✅ Fixed

- **Data Safe TNS_ADMIN Isolation**
  - `oraenv.sh` now enforces connector-specific `TNS_ADMIN` via the Data Safe plugin
  - `oradba_dsctl.sh` sets per-connector `TNS_ADMIN` before cmctl operations
  - Prevents connectors from sharing `cman.ora` across environments

## ➕ Added

- **Data Safe Plugin Environment Helper**
  - Added `plugin_set_environment()` to enforce connector-specific variables

- **Data Safe Plugin Tests**
  - Added coverage for `TNS_ADMIN` handling and connector isolation

## 🔄 Changed

- **User Documentation**
  - Documented Data Safe connector-specific `TNS_ADMIN` behavior

## Files Updated

- `VERSION`
- `CHANGELOG.md`
- `doc/releases/v0.20.2.md`
- `src/bin/oraenv.sh`
- `src/bin/oradba_dsctl.sh`
- `src/bin/oradba_env.sh`
- `src/lib/plugins/datasafe_plugin.sh`
- `tests/test_datasafe_plugin.bats`
- `src/doc/environment.md`

## Upgrade Notes

No breaking changes. Update and run your usual installation or sync process.
